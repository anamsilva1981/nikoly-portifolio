import { TechToolName } from "@data/schema/tech-tool-name.schema";

export interface LanguageTool {
  name: TechToolName;
  url: string;
}


export interface LanguagesToolsSchema {
  icon: any;
  name: string;
  category: string;
}
