import { ProfileSchema } from "@data/schema/profile.schema";

const profileData: ProfileSchema = {
  name: 'Nikoly Stradiotto',
  bio: 'a passionate game design student',
  // resumeLink: 'cv.pdf',
  greetings: [
    'Kumusta, mundo!',
    'Hello, world!'
  ],
  pronouns: 'She/Her/Hers'
}

export default profileData;